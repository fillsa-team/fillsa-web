# 컴포넌트 스펙 (컴포넌트 ↔ Figma 노드 ↔ props)

> 디자이너가 작성. 프론트는 이 스펙대로 구현한다.

## Button
- Figma 노드: (링크)
- props: `variant: 'primary' | 'secondary'`, `size: 'sm' | 'md'`, `disabled?: boolean`
- 토큰: color.primary, radius.md, spacing.sm
